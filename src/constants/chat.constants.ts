/** Sentinel id for the unsaved new-conversation shell state. */
export const NEW_CONVERSATION_ID = '__new__'

/** Stable id for the single persistent conversation (whole thread). */
export const THREAD_SESSION_ID = '__thread__'

/** Max characters for sidebar conversation titles before ellipsis. */
export const SESSION_TITLE_MAX_LENGTH = 40
